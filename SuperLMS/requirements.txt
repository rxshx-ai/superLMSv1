requests>=2.31.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# AWS Bedrock (any model via the Converse API) — primary LLM provider
boto3>=1.35.0

# Groq (Llama) — optional fallback, safe to remove if not needed
groq>=0.9.0
